# ===================================================================
# Test run — paper1 physics, PS-only
# ===================================================================

energy_eV: 100.0e3
pitch_deg: 30.0

# Integration defined by total steps rather than gyroperiods.
gyroperiods: null         # null suppresses base.yml's value so total_steps drives norm_time
total_steps: 1.0e7

window_time: 6209.0
